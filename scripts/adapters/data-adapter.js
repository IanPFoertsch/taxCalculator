'use-strict';

//the data adapter is a tree crawler that outputs chart-formatted data
//to the chartjsAdapter

function DataAdapter() {}

var netWorthData = function(person) {
  //line chart conversion
  //need data in an [{x:index y:val}, {x:index y:val}] format
  //Need the time indexes: query the person
  var result = {}
  return result
}

DataAdapter.netWorthData = netWorthData

var taxFlowData = function() {
  //need data in a
}

DataAdapter.fun = function(type) {

}


Adapters.DataAdapter = DataAdapter
